# =============================================================================
# MailForge Development Docker Compose
# =============================================================================
#
# Usage:
#   docker compose -f docker-compose.dev.yml up -d
#   docker compose -f docker-compose.dev.yml logs -f
#   docker compose -f docker-compose.dev.yml down
#
# Features:
#   - Hot reload for API and Worker
#   - MailHog for email testing (UI at http://localhost:8025)
#   - Debug ports exposed
#   - Volume mounts for source code
#   - Lower resource limits
#
# =============================================================================

services:
  # ===========================================================================
  # PostgreSQL Database (Development)
  # ===========================================================================
  postgres:
    image: postgres:16-alpine
    container_name: mailforge-postgres-dev
    restart: unless-stopped
    volumes:
      - pgdata-dev:/var/lib/postgresql/data
      - ./packages/shared/src/db/migrations:/docker-entrypoint-initdb.d/migrations:ro
      - ./scripts/init-db.sh:/docker-entrypoint-initdb.d/01-init-db.sh:ro
    environment:
      POSTGRES_USER: mailforge
      POSTGRES_PASSWORD: mailforge
      POSTGRES_DB: mailforge
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mailforge -d mailforge"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - mailforge-dev

  # ===========================================================================
  # Redis (Development)
  # ===========================================================================
  redis:
    image: redis:7-alpine
    container_name: mailforge-redis-dev
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - mailforge-dev

  # ===========================================================================
  # MailHog - Email Testing (captures all outbound emails)
  # ===========================================================================
  mailhog:
    image: mailhog/mailhog:latest
    container_name: mailforge-mailhog
    restart: unless-stopped
    ports:
      - "1025:1025"   # SMTP port
      - "8025:8025"   # Web UI
    environment:
      MH_STORAGE: memory
      MH_SMTP_BIND_ADDR: 0.0.0.0:1025
      MH_UI_BIND_ADDR: 0.0.0.0:8025
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8025"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - mailforge-dev
    labels:
      - "com.mailforge.service=mailhog"
      - "com.mailforge.description=Email testing UI at http://localhost:8025"

  # ===========================================================================
  # MailForge API Server (Development with hot reload)
  # ===========================================================================
  api:
    build:
      context: .
      dockerfile: packages/api/Dockerfile
      target: builder  # Use builder stage for dev dependencies
    container_name: mailforge-api-dev
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://mailforge:mailforge@postgres:5432/mailforge
      API_SECRET: dev-secret-change-in-production
      API_PORT: 3000
      API_HOST: 0.0.0.0
      REDIS_URL: redis://redis:6379
      NODE_ENV: development
      LOG_LEVEL: debug
      LOG_FORMAT: pretty
      DEBUG: "true"
      SKIP_DNS_VERIFICATION: "true"
      TEST_MODE: "false"
      # Lower rate limits for development
      RATE_LIMIT_PER_MINUTE: 1000
      RATE_LIMIT_PER_HOUR: 10000
      RATE_LIMIT_PER_DAY: 100000
    volumes:
      # Mount source for hot reload
      - ./packages/api/src:/app/packages/api/src:ro
      - ./packages/shared/src:/app/packages/shared/src:ro
    ports:
      - "3000:3000"
      - "9229:9229"   # Node.js debugger port
    command: ["bun", "run", "--watch", "packages/api/src/index.ts"]
    networks:
      - mailforge-dev
    labels:
      - "com.mailforge.service=api"
      - "com.mailforge.environment=development"

  # ===========================================================================
  # MailForge Background Worker (Development)
  # ===========================================================================
  worker:
    build:
      context: .
      dockerfile: packages/worker/Dockerfile
      target: builder
    container_name: mailforge-worker-dev
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mailhog:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://mailforge:mailforge@postgres:5432/mailforge
      REDIS_URL: redis://redis:6379
      # Use MailHog instead of real SMTP
      SMTP_HOST: mailhog
      SMTP_PORT: 1025
      WORKER_CONCURRENCY: 5
      MAX_RETRIES: 3
      RETRY_DELAY_MS: 1000
      POLL_INTERVAL_MS: 2000
      NODE_ENV: development
      LOG_LEVEL: debug
    volumes:
      - ./packages/worker/src:/app/packages/worker/src:ro
      - ./packages/shared/src:/app/packages/shared/src:ro
    command: ["node", "--watch", "packages/worker/src/index.ts"]
    networks:
      - mailforge-dev
    labels:
      - "com.mailforge.service=worker"
      - "com.mailforge.environment=development"

  # ===========================================================================
  # Haraka SMTP Server (Development - for testing inbound)
  # ===========================================================================
  smtp:
    build:
      context: haraka
      dockerfile: Dockerfile
    container_name: mailforge-smtp-dev
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      HARAKA_HOSTNAME: mail.localhost
      DKIM_SELECTOR: mailforge
      MAILFORGE_API_URL: http://api:3000
      DATABASE_URL: postgres://mailforge:mailforge@postgres:5432/mailforge
      NODE_ENV: development
      HARAKA_LOG_LEVEL: debug
    ports:
      - "2525:25"      # Non-privileged port for development
      - "2587:587"     # Submission port
    volumes:
      - ./haraka/config:/app/config:ro
      - ./haraka/plugins:/app/plugins:ro
      - ./keys:/app/keys:ro
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "25"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks:
      - mailforge-dev
    labels:
      - "com.mailforge.service=smtp"
      - "com.mailforge.environment=development"

  # ===========================================================================
  # MCP Server (Development)
  # ===========================================================================
  mcp-server:
    build:
      context: .
      dockerfile: packages/mcp-server/Dockerfile
      target: builder
    container_name: mailforge-mcp-dev
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    environment:
      MAILFORGE_API_URL: http://api:3000
      MAILFORGE_API_KEY: mf_dev_test_key
      MCP_PORT: 3001
      MCP_HOST: 0.0.0.0
      NODE_ENV: development
      LOG_LEVEL: debug
    volumes:
      - ./packages/mcp-server/src:/app/packages/mcp-server/src:ro
      - ./packages/shared/src:/app/packages/shared/src:ro
    ports:
      - "3001:3001"
    networks:
      - mailforge-dev
    labels:
      - "com.mailforge.service=mcp"
      - "com.mailforge.environment=development"

  # ===========================================================================
  # pgAdmin (Optional - Database GUI)
  # ===========================================================================
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: mailforge-pgadmin
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@mailforge.dev
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "5050:80"
    volumes:
      - pgadmin-data:/var/lib/pgadmin
    networks:
      - mailforge-dev
    profiles:
      - tools  # Only start with: docker compose --profile tools up
    labels:
      - "com.mailforge.service=pgadmin"
      - "com.mailforge.description=Database UI at http://localhost:5050"

  # ===========================================================================
  # Redis Commander (Optional - Redis GUI)
  # ===========================================================================
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: mailforge-redis-commander
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      REDIS_HOSTS: local:redis:6379
    ports:
      - "8081:8081"
    networks:
      - mailforge-dev
    profiles:
      - tools
    labels:
      - "com.mailforge.service=redis-commander"
      - "com.mailforge.description=Redis UI at http://localhost:8081"

# =============================================================================
# Networks
# =============================================================================
networks:
  mailforge-dev:
    driver: bridge

# =============================================================================
# Volumes
# =============================================================================
volumes:
  pgdata-dev:
    driver: local
  pgadmin-data:
    driver: local
